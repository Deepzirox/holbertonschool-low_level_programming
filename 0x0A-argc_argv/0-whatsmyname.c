#include <stdio.h>
/**
 * main - Entry point (program)
 * @argc: number of commands
 * @argv: array of commands
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - 1]);
	return (0);
}
